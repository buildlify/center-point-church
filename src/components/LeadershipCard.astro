---
const { person } = Astro.props;
---

<div class="mt-3 mb-2 flex w-5/6 flex-col sm:mx-6 sm:mb-4 sm:w-80">
  <img
    src={`/leadership/${person.img}`}
    alt={`${person.name} ${person.role}`}
    class="rounded-primary shadow-inset aspect-image h-full w-full object-cover"
  />
  <div class="text-center sm:text-left">
    <h3 class="text-xl font-semibold">{person.name}</h3>
    <h4 class="text-primary italic">{person.role}</h4>
  </div>
</div>
